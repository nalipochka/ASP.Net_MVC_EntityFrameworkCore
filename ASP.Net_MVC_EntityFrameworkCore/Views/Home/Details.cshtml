@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model Book
@section Styles{
    <style>
        .about {
            display: inline-block;
            text-align:left;
            max-width:1000px;
            margin: 20px auto;
        }

        .btn {
            margin: 20px;
            width:300px;
            height: 50px;
            font-size: 20px;
            display: table-cell;
            vertical-align: middle;
        }
        img{
            float:left;
            margin-right: 20px;
            margin-bottom:10px;
        }
        
    </style>
}
    @*<img src=@($"data:image/*;base64,{Convert.ToBase64String(Model?.Photo)}") alt="@Model.Title" />*@

    <div class="about">
        <p> <img src="@Model.FilePath" , alt="@Model.Title" /> @Model.More </p>
    </div>

<div class="button">
    <a asp-action="Library" class="btn btn-sm btn-primary">Return to books</a>
</div>
